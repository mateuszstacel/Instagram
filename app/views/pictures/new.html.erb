<%= form_for @picture do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
<br>
  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.button :submit %>

<% end %>

<%= link_to "back", @picture %>
